# SPDX-License-Identifier: Apache-2.0

# Logging
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=n
# Increase log buffer to avoid dropped messages
CONFIG_LOG_BUFFER_SIZE=2048
CONFIG_LOG_PRINTK=y

# SD Card and File System
CONFIG_DISK_ACCESS=y
CONFIG_DISK_DRIVER_SDMMC=y
CONFIG_FILE_SYSTEM=y
CONFIG_FAT_FILESYSTEM_ELM=y
CONFIG_FS_FATFS_LFN=y
# Use HEAP mode for LFN to enable thread-safe FATFS
CONFIG_FS_FATFS_LFN_MODE_HEAP=y
# Enable FATFS reentrant/thread-safe operations for concurrent read/write
CONFIG_FS_FATFS_REENTRANT=y
CONFIG_FS_FATFS_MKFS=y
CONFIG_FILE_SYSTEM_MKFS=y

# I2C for potential sensor communication
CONFIG_I2C=y

# Sensor framework
CONFIG_SENSOR=y

# GPIO for buttons
CONFIG_GPIO=y

# Custom GPIO button driver
CONFIG_GPIO_BUTTON=y
CONFIG_INPUT_GPIO_BUTTON_OWN_THREAD=y
CONFIG_INPUT_GPIO_BUTTON_THREAD_STACK_SIZE=2048
CONFIG_INPUT_GPIO_BUTTON_THREAD_PRIORITY=5

# Regulator for SD card power control
CONFIG_REGULATOR=y

# Audio configuration
CONFIG_AUDIO=y
CONFIG_AUDIO_DMIC=y

# Opus codec library
CONFIG_OPUS_CODEC=y

# SpeexDSP audio processing library (noise suppression, dereverb)
CONFIG_SPEEXDSP=y

# Main stack size (increased for AT command processing)
CONFIG_MAIN_STACK_SIZE=16384

# Enable thread naming for debugging
CONFIG_THREAD_NAME=y

# Heap for JSON and buffers
CONFIG_HEAP_MEM_POOL_SIZE=65536


# Bluetooth BLE configuration
CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_DEVICE_NAME="reSpeaker Clip"
CONFIG_BT_MAX_CONN=1
CONFIG_BT_GATT_CLIENT=y

# BLE throughput optimization
CONFIG_BT_BUF_ACL_RX_SIZE=502
CONFIG_BT_BUF_ACL_TX_SIZE=502
CONFIG_BT_L2CAP_TX_MTU=247

# BLE RX stack size
CONFIG_BT_RX_STACK_SIZE=2048

# Flash and NVS for configuration storage
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_SETTINGS=y
CONFIG_SETTINGS_RUNTIME=n
CONFIG_NVS=y
# Enable settings to use NVS backend
CONFIG_SETTINGS_NVS=y
# Enable reboot for AT+REBOOT command
CONFIG_REBOOT=y

